# Copy to .env.local for local dev. In production, set this in Vercel
# (Project Settings -> Environment Variables), never commit real values.

# Shared password for the site-wide login gate (see src/proxy.ts).
# Pick any string; share it out-of-band with people you send the link to.
# Optional — the gate fails open (no login required) if left unset.
SITE_PASSWORD=
